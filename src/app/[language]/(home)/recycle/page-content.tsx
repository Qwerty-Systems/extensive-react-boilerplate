import SectionComponent from "@/components/SectionComponent";
import { RecyclersData } from "../sectionData";

export default function RecyclersPage() {
  return <SectionComponent {...RecyclersData} />;
}
