import SectionComponent from "@/components/SectionComponent";
import { MarketplaceData } from "../sectionData";

export default function MarketplacePage() {
  return <SectionComponent {...MarketplaceData} />;
}
